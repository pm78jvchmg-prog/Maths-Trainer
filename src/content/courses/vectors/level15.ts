/** Vectors, level 15 (`vm-l15`). Placeholder, written by its own worker. */
import type { Level } from '../../types';

export const level15: Level = {
  id: 'vm-l15',
  title: 'Level 15',
  lessons: [],
};
