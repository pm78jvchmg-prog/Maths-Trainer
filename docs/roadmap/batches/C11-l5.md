# C11-l5: Number & Proof: Euclid's Algorithm

Status: done
Branch: `claude/roadmap-c11-number-proof-l5-polgo4`

Level `np-l5` in course `number-proof`, 5 lessons (the algorithm, reading a run, working backwards, solving ax + by = c, all the solutions), a level check of 15 questions, 23 new generators (`euc-*`) in `generators/numberEuclid.ts`, two of them `order` proofs added to `proofOrder.test.ts`, and `numberEuclid.test.ts` re-running the algorithm from each slide's own numbers and checking every quoted quotient, remainder, HCF and solution by substitution.
