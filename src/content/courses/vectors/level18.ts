/** Vectors, level 18 (`vm-l18`). Placeholder, written by its own worker. */
import type { Level } from '../../types';

export const level18: Level = {
  id: 'vm-l18',
  title: 'Level 18',
  lessons: [],
};
