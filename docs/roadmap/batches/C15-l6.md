# C15-l6: Numerical Methods: Choosing a Method

Status: done
Branch: `claude/roadmap-c15-numerical-l6-li6qqm`

Level `nm-l6` in course `numerical-methods`, 5 lessons (Interval Bisection, Side by Side, Speed of Convergence, When Each Breaks, Which to Reach For), a level check of 15 questions, 17 new generators (`numer-bisect-*`, `numer-side-*`, `numer-nearest-method`, `numer-speed-*`, `numer-breaks-*`, `numer-reach-*`). Bisection and the side-by-side comparison use the `table` kind; iterates are written to 4 places through `written` and refused when carrying the rounded value forward would change a row. No slide types a derivative, so none declares `source`; `numericalMethods.test.ts` reads f, the bracket, g and the start off each slide and runs bisection, iteration and Newton-Raphson itself.
