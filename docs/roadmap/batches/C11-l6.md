# C11-l6: Number & Proof: Modular Arithmetic

Status: done
Branch: `claude/roadmap-c11-number-proof-l6-6rxby7`

Level `np-l6` in course `number-proof`, 5 lessons (congruence mod n, adding and multiplying, powers mod n, last digits, why digit sums work), a level check of 15 questions, 24 new generators (`cong-*`) in `generators/numberModular.ts`, two of them `order` proofs (`cong-arith-order`, `cong-digit-order`) added to `proofOrder.test.ts`, and `numberModular.test.ts` re-checking every residue, last digit and digit sum from each slide's own numbers, with every power re-done by repeated multiplication.
