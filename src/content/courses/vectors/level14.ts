/** Vectors, level 14 (`vm-l14`). Placeholder, written by its own worker. */
import type { Level } from '../../types';

export const level14: Level = {
  id: 'vm-l14',
  title: 'Level 14',
  lessons: [],
};
