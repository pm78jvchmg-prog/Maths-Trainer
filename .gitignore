# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code config. The shared settings file and the skills, agents and
# commands directories are committed so that cloud sessions pick up the
# subagent model policy, the project skills, the review agents and their slash
# commands; everything else here is local.
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/agents/
!.claude/commands/
.claude/settings.local.json

# Local reference screenshots/video, duplicated under docs/inspiration/
/Game Inspiration/
eval/scorers/
