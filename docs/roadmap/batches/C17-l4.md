# C17-l4: Data, Averages and Spread: Cumulative Frequency

Status: done
Branch: `claude/roadmap-c17-data-l4-qkn9a8`

Level `da-l4` in course `data-averages-spread`, 5 lessons, a level check of 15 questions, 20 new generators (`dat-cf-*`, `dat-pct-*`, `dat-interp-*`).

The curve is `cumulativeSvg` in `generators/dataAveragesSpread.ts`: plotSvg in squares with the scale laid over it as SVG text, points joined by straight lines so a reading off the curve is exactly the interpolation of lesson 5. No widget was added; three generators read it by slider. Level 3 (Representing Data) had not landed, so this level sits after `da-l2` and level 3 slots in between when it does.
