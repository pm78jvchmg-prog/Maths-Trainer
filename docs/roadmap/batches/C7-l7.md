# C7-l7: Binomial Expansion: Approximating with the Series

Status: done
Branch: `claude/roadmap-c7-binomial-l7-vxeblr`

Level `be-l7`, 5 lessons, a 15-question level check, 20 new generators and a block in `binomialExpansion.test.ts` that reads each number, bracket, n and x back off the slide, sums the kept terms itself, and holds every estimate to that sum and to within the first term left out of mathjs evaluating the number. A root from the series (√1.02 from (1 + x)^{1/2}), reciprocals (1/0.98² and 1/1.04 as negative powers), taking the number out first (√4.08 = 2(1 + 0.02)^{1/2}, ∛8.24 likewise), how good an estimate is and which x to choose (√2 from √0.98 = 7√2/10), and the series of a partial-fraction split, pointing at `af-l2-two` for the split. Every estimate is exact: decimals are written from exact fractions in whole-number arithmetic (`qDec`, `qRound`), never through a float.
